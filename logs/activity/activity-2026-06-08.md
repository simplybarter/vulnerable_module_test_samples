## [2026-06-08 12:04]

### Language

- `csharp`

### Workflow Assessment

- Current coverage: `legacy/` covers `packages.config` and `msbuild/` covers SDK-style `PackageReference` in `.csproj`; both current fixtures are `manifest` only.
- Gap reviewed: NuGet lock file coverage via `packages.lock.json`.
- Why it matters: the repository currently exercises direct declaration parsing for C# but does not exercise locked dependency parsing or restore-graph pinning for NuGet projects.
- Decision: use `packages.lock.json` as the highest-value next C# fixture candidate for a future implementation pass.

### Research Sources

- Microsoft Learn, "PackageReference in project files": https://learn.microsoft.com/en-us/nuget/consume-packages/package-references-in-project-files
- Microsoft Learn, "What is NuGet and what does it do?": https://learn.microsoft.com/en-us/nuget/what-is-nuget
- GitHub Advisory Database, "CVE-2024-21907 / GHSA-5crp-9r3c-p9vr": https://github.com/advisories/GHSA-5crp-9r3c-p9vr
- NuGet Gallery, `Newtonsoft.Json`: https://www.nuget.org/packages/Newtonsoft.Json/

### Vulnerable Package Selection

- Package: `Newtonsoft.Json`
- Version: `12.0.3`
- Rationale: the GitHub Advisory Database marks `Newtonsoft.Json` versions earlier than `13.0.1` as affected by CVE-2024-21907, and the NuGet Gallery explicitly flags `12.0.3` as having at least one high-severity vulnerability. It is also a common, recognizable NuGet package.

### Implementation

- Files added: none in this dry run
- Files updated: none in this dry run
- Fixture behavior introduced: none; this was a workflow validation run of the skill rather than a corpus expansion change

### Proposed File Changes

- Would add `fixtures/csharp/lockfile/App.csproj` with `PackageReference` entries and lock-file settings to exercise NuGet locked dependency parsing.
- Would add `fixtures/csharp/lockfile/packages.lock.json` to represent the resolved NuGet dependency graph for the fixture.
- Would add `fixtures/csharp/lockfile/expected.json` with `NuGet` `locked` entries derived from `packages.lock.json`.
- Would update `fixtures/csharp/README.md` to document the new `lockfile/` fixture, files under test, and the locked-dependency behavior being exercised.

### expected.json Assessment

- Ecosystem label: `NuGet`
- Discovery types used: none added in this dry run; the candidate future fixture would primarily exercise `locked`
- Validation notes: no `expected.json` file was created in this test run

### Documentation Impact

- Language README updates: in a real implementation, `fixtures/csharp/README.md` would gain a new `lockfile/` row, expected dependency notes, and a short explanation of why `packages.lock.json` belongs in the corpus
- Root documentation updates: none in this dry run
- Template or schema updates: none in this dry run

### Repository Impact

- Scanner behaviors now covered: none added yet; the run validated that the skill can audit current coverage, identify a gap, choose a package example, and produce a timestamped activity report
- Remaining gaps: C# still lacks a lockfile fixture and any vendored or installed-package metadata fixture
- Risks or follow-up: when implementing the future fixture, keep it minimal and application-oriented because Microsoft recommends checking lock files into source repositories for app entry points but not for reusable library projects

## [2026-06-08 12:11]

### Language

- `csharp`

### Workflow Assessment

- Current coverage: `legacy/` and `msbuild/` already covered manifest parsing; no prior C# lockfile fixture existed.
- Gap reviewed: NuGet lock file coverage via `packages.lock.json`.
- Why it matters: this adds explicit `locked` coverage for C# and exercises a resolved dependency graph rather than only direct declarations.
- Decision: implemented `lockfile/` as a minimal application-style fixture with a checked-in lock file.

### Research Sources

- Microsoft Learn, "PackageReference in project files": https://learn.microsoft.com/en-us/nuget/consume-packages/package-references-in-project-files
- Microsoft Learn, "What is NuGet and what does it do?": https://learn.microsoft.com/en-us/nuget/what-is-nuget
- GitHub Advisory Database, "CVE-2024-21907 / GHSA-5crp-9r3c-p9vr": https://github.com/advisories/GHSA-5crp-9r3c-p9vr
- NuGet Gallery, `Newtonsoft.Json`: https://www.nuget.org/packages/Newtonsoft.Json/

### Vulnerable Package Selection

- Package: `Newtonsoft.Json`
- Version: `12.0.3`
- Rationale: the package is common in C# repositories, the advisory covers versions earlier than `13.0.1`, and the NuGet Gallery flags `12.0.3` as having at least one high-severity vulnerability.

### Implementation

- Files added:
  - `fixtures/csharp/lockfile/App.csproj`
  - `fixtures/csharp/lockfile/packages.lock.json`
  - `fixtures/csharp/lockfile/expected.json`
- Files updated:
  - `fixtures/csharp/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: locked NuGet dependency extraction from `packages.lock.json`, including one historically vulnerable direct package and multiple transitive locked packages.

### expected.json Assessment

- Ecosystem label: `NuGet`
- Discovery types used: `locked`
- Validation notes: `expected.json` was limited to packages explicitly listed in `packages.lock.json`, and each entry uses the required `name`, `version`, `eco`, and `type` fields.

### Documentation Impact

- Language README updates: added the new `lockfile/` fixture row, expected dependencies, and notes about why this fixture exists.
- Root documentation updates: updated the `fixtures/` layout tree and C# coverage row to include `lockfile/`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: NuGet lockfile parsing, locked dependency graph extraction, and differentiation between manifest-only and locked dependency evidence in C# fixtures.
- Remaining gaps: C# still lacks an installed or vendored package metadata fixture.
- Risks or follow-up: the lock file uses placeholder content hashes because the fixture is intended for static parsing tests rather than package restore validation.

## [2026-06-08 12:54]

### Language

- `node`

### Workflow Assessment

- Current coverage: `bun/`, `deno/`, and `npm/` already covered three lockfile styles, but the repository had no `pnpm` fixture.
- Gap reviewed: `pnpm-lock.yaml` coverage for the Node ecosystem.
- Why it matters: `pnpm` is a major package manager with a deterministic lockfile that differs structurally from `package-lock.json`, especially through its `importers` and `packages` sections.
- Decision: implemented a `pnpm/` fixture centered on `pnpm-lock.yaml` plus a minimal `package.json`.

### Research Sources

- pnpm GitHub repository README: https://github.com/pnpm/pnpm
- pnpm project homepage: https://pnpm.io/
- GitHub Advisory Database, "CVE-2021-23337 / GHSA-35jh-r3h4-6jhm": https://github.com/advisories/GHSA-35jh-r3h4-6jhm

### Vulnerable Package Selection

- Package: `lodash`
- Version: `4.17.20`
- Rationale: `lodash` is a common npm package, the GitHub advisory marks versions earlier than `4.17.21` as affected, and `4.17.20` is a clean historically vulnerable version for a static fixture.

### Implementation

- Files added:
  - `fixtures/node/pnpm/package.json`
  - `fixtures/node/pnpm/pnpm-lock.yaml`
  - `fixtures/node/pnpm/expected.json`
- Files updated:
  - `fixtures/node/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: locked dependency extraction from `pnpm-lock.yaml`, covering direct packages and one transitive dependency through the `packages` map.

### expected.json Assessment

- Ecosystem label: `node`
- Discovery types used: `locked`
- Validation notes: `expected.json` includes only packages explicitly represented in the `pnpm-lock.yaml` importer and package entries, and keeps the result minimal and deterministic.

### Documentation Impact

- Language README updates: added the `pnpm/` fixture row, expected package notes, and lockfile-specific behavior.
- Root documentation updates: updated the Node layout tree and coverage row to include `pnpm`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: `pnpm-lock.yaml` parsing, direct importer dependency extraction, and package-map-based transitive dependency extraction for Node fixtures.
- Remaining gaps: Node still lacks coverage for other package-manager patterns such as Yarn lockfiles or workspace-oriented Node dependency scenarios.
- Risks or follow-up: the lockfile uses placeholder integrity hashes because the corpus is focused on static dependency-discovery parsing, not installation fidelity.

## [2026-06-08 13:01]

### Language

- `java`

### Workflow Assessment

- Current coverage: `gradle/` and `maven/` covered manifest parsing, but Java had no lockfile-style fixture.
- Gap reviewed: Gradle dependency locking via `gradle.lockfile`.
- Why it matters: Gradle lock state is structurally different from manifest-only Gradle and Maven declarations, and it records resolved versions for direct and transitive dependencies.
- Decision: implemented a `gradle-lockfile/` fixture with a checked-in `gradle.lockfile` and a dynamic dependency selector pinned by lock state.

### Research Sources

- Gradle User Manual, "Locking Versions": https://docs.gradle.org/current/userguide/dependency_locking.html
- GitHub Advisory Database, "Remote code injection in Log4j / CVE-2021-44228 / GHSA-jfh8-c2jp-5v3q": https://github.com/advisories/GHSA-jfh8-c2jp-5v3q
- Gradle blog, "Dealing with the Critical Log4j Vulnerability": https://blog.gradle.org/log4j-vulnerability

### Vulnerable Package Selection

- Package: `org.apache.logging.log4j:log4j-core`
- Version: `2.14.1`
- Rationale: `2.14.1` is the widely recognized Log4Shell-era vulnerable version, and it is suitable for a Java lockfile fixture because its transitive `log4j-api` relationship is also easy to represent deterministically.

### Implementation

- Files added:
  - `fixtures/java/gradle-lockfile/build.gradle`
  - `fixtures/java/gradle-lockfile/gradle.lockfile`
  - `fixtures/java/gradle-lockfile/expected.json`
- Files updated:
  - `fixtures/java/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: locked dependency extraction from Gradle lock state, including a dynamic version selector in `build.gradle` that resolves to a pinned vulnerable module in `gradle.lockfile`.

### expected.json Assessment

- Ecosystem label: `Maven`
- Discovery types used: `locked`
- Validation notes: `expected.json` includes only modules explicitly present in `gradle.lockfile` and records them as `groupId:artifactId` names, consistent with existing Java fixtures.

### Documentation Impact

- Language README updates: added the `gradle-lockfile/` fixture row, expected package notes, and lockfile-specific behavior.
- Root documentation updates: updated the Java layout tree and coverage row to include `gradle-lockfile`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: Gradle lockfile parsing, direct and transitive module extraction from lock state, and handling of a manifest range pinned by `gradle.lockfile`.
- Remaining gaps: Java still lacks a vendored or installed metadata fixture.
- Risks or follow-up: the fixture focuses on the lockfile contract rather than full Gradle project realism, so future Java additions should stay similarly narrow.

## [2026-06-08 13:09]

### Language

- `go`

### Workflow Assessment

- Current coverage: `mod/` covered `go.mod` and `go.sum`, but the repository had no vendoring-oriented Go fixture.
- Gap reviewed: vendored module metadata via `vendor/modules.txt`.
- Why it matters: when vendoring is enabled, the Go toolchain uses `vendor/modules.txt` as module version metadata, which is materially different from reading only `go.mod` and `go.sum`.
- Decision: implemented a `vendor/` fixture centered on `vendor/modules.txt` and a minimal checked-in vendor tree.

### Research Sources

- Go Modules Reference, vendoring and `vendor/modules.txt`: https://tip.golang.org/ref/mod
- Go Vulnerability Database overview: https://go.dev/doc/security/vuln/database
- Go vulnerability report GO-2023-1571: https://pkg.go.dev/vuln/GO-2023-1571

### Vulnerable Package Selection

- Package: `golang.org/x/net`
- Version: `0.6.0`
- Rationale: the Go vulnerability report GO-2023-1571 marks `golang.org/x/net/http2` and `golang.org/x/net/http2/hpack` as affected before `v0.7.0`, making `v0.6.0` a clean historically vulnerable vendored module example.

### Implementation

- Files added:
  - `fixtures/go/vendor/go.mod`
  - `fixtures/go/vendor/expected.json`
  - `fixtures/go/vendor/vendor/modules.txt`
  - `fixtures/go/vendor/vendor/golang.org/x/net/http2/doc.go`
  - `fixtures/go/vendor/vendor/golang.org/x/text/transform/doc.go`
- Files updated:
  - `fixtures/go/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: vendored module discovery from `vendor/modules.txt`, covering one explicit vulnerable vendored module and one transitive vendored module.

### expected.json Assessment

- Ecosystem label: `Go`
- Discovery types used: `installed`
- Validation notes: `expected.json` was limited to module versions explicitly represented by `vendor/modules.txt`, and the vendored package paths align with those modules.

### Documentation Impact

- Language README updates: added the `vendor/` fixture row, expected package notes, and vendoring-specific behavior.
- Root documentation updates: updated the Go layout tree and coverage row to include `vendor/`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: vendored Go module discovery, `vendor/modules.txt` parsing, and differentiation between locked module-file evidence and vendored installed-module evidence.
- Remaining gaps: Go still lacks broader multi-module or workspace-oriented fixture scenarios.
- Risks or follow-up: the vendored package files are intentionally minimal placeholders because the corpus is testing metadata discovery, not full source fidelity.

## [2026-06-08 19:15]

### Language

- `zig`

### Workflow Assessment

- Current coverage: `zig/` covered root `build.zig.zon` manifest parsing only, and the language README explicitly noted the lack of a vendored or installed Zig sample.
- Gap reviewed: checked-in fetched package metadata under the project-local `zig-pkg/` directory that Zig now uses for fetched dependencies.
- Why it matters: Zig projects can now keep fetched packages in `zig-pkg/` next to the project root, and those directories can be distributed for offline or archival builds. Scanners that only inspect the root `build.zig.zon` will miss installed-package evidence.
- Decision: implemented `zig-pkg/` as a mixed manifest and installed fixture using a root `build.zig.zon` plus nested fetched package `build.zig.zon` files under `zig-pkg/`.

### Research Sources

- Zig 0.11.0 release notes, package management and `build.zig.zon`: https://ziglang.org/download/0.11.0/release-notes.html
- Zig 2026 devlog, fetched packages stored in `zig-pkg/`: https://ziglang.org/devlog/2026/
- zigzap/zap releases, `zig fetch --save` usage and tagged versions: https://github.com/zigzap/zap/releases

### Vulnerable Package Selection

- Package selection result: no authoritative Zig package vulnerability database or package-specific published security advisory trail was identified during this research pass for the selected fixture candidates.
- Chosen representative packages: `zap` `0.8.0` and `hzzp` `0.1.2`
- Rationale: the current corpus already uses these real Zig packages, and reusing them for the installed-metadata gap adds value without making an unsupported vulnerability claim for the Zig ecosystem.

### Implementation

- Files added:
  - `fixtures/zig/zig-pkg/build.zig.zon`
  - `fixtures/zig/zig-pkg/expected.json`
  - `fixtures/zig/zig-pkg/zig-pkg/zap-0.8.0-alzUkTyBqgBwke4Jsot997WYSpl207Ij9oO-2QOvGrOi/build.zig.zon`
  - `fixtures/zig/zig-pkg/zig-pkg/hzzp-0.1.2-vuF-cMAkAADVsm707MYCtPmqmRs0gzg84Sz0qGbb5E3w/build.zig.zon`
- Files updated:
  - `fixtures/zig/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: mixed manifest and installed dependency extraction for Zig projects that keep fetched packages checked into `zig-pkg/`.

### expected.json Assessment

- Ecosystem label: `Zig`
- Discovery types used: `manifest`, `installed`
- Validation notes: `expected.json` records the direct package declarations from the root `build.zig.zon` and the exact package names and versions from nested fetched package `build.zig.zon` files.

### Documentation Impact

- Language README updates: added the `zig-pkg/` fixture row, expected package notes, and installed-metadata behavior.
- Root documentation updates: updated the Zig layout tree and coverage row to include `zig-pkg`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: Zig fetched-package discovery under `zig-pkg/`, nested `build.zig.zon` parsing for installed metadata, and distinction between declared and installed package evidence in Zig fixtures.
- Remaining gaps: Zig still lacks a separately modeled lockfile concept in this corpus, but the installed-package metadata gap is now covered using the official fetched-package layout.
- Risks or follow-up: the fetched package directories are intentionally minimal metadata stubs rather than full source trees because the corpus is testing dependency discovery, not Zig build execution.

## [2026-06-08 19:09]

### Language

- `rust`

### Workflow Assessment

- Current coverage: `cargo/` covered manifest parsing and `cargo-vendor/` covered vendored installed metadata, but there was no fixture for direct `Cargo.lock` parsing.
- Gap reviewed: exact resolved crate extraction from `Cargo.lock`, paired with a broad version requirement in `Cargo.toml`.
- Why it matters: the Cargo book explicitly distinguishes `Cargo.toml` from `Cargo.lock`, and Rust security tooling commonly audits `Cargo.lock`. Scanners that only parse manifests or vendored crates will miss resolved lockfile state.
- Decision: implemented `cargo-lock/` as a mixed manifest and lockfile fixture using a broad `idna = "0.5"` declaration plus a checked-in `Cargo.lock`.

### Research Sources

- Cargo Book, `Cargo.toml` vs `Cargo.lock`: https://doc.rust-lang.org/nightly/cargo/guide/cargo-toml-vs-cargo-lock.html
- RustSec advisory for `idna`: https://rustsec.org/advisories/RUSTSEC-2024-0421.html
- Docs.rs crate metadata for `idna` `0.5.0`: https://docs.rs/crate/idna/0.5.0

### Vulnerable Package Selection

- Package: `idna`
- Version: `0.5.0`
- Rationale: RustSec marks `idna` `0.5.0` and earlier as affected by RUSTSEC-2024-0421 / CVE-2024-12224, making `0.5.0` a precise historically vulnerable crate version for a lockfile fixture.

### Implementation

- Files added:
  - `fixtures/rust/cargo-lock/Cargo.toml`
  - `fixtures/rust/cargo-lock/Cargo.lock`
  - `fixtures/rust/cargo-lock/expected.json`
- Files updated:
  - `fixtures/rust/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: mixed manifest and locked dependency extraction for Cargo projects, including one vulnerable direct crate and a small transitive Unicode-related lock graph.

### expected.json Assessment

- Ecosystem label: `crates.io`
- Discovery types used: `manifest`, `locked`
- Validation notes: `expected.json` records the broad direct declaration from `Cargo.toml` and the exact crate versions listed in `Cargo.lock`, excluding the root package itself.

### Documentation Impact

- Language README updates: added the `cargo-lock/` fixture row, expected crate notes, and lockfile-specific behavior.
- Root documentation updates: updated the Rust layout tree and coverage row to include `cargo-lock`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: direct `Cargo.lock` parsing, distinction between declared crate requirements and exact locked versions, and recursive lockfile dependency extraction in Rust fixtures.
- Remaining gaps: Rust still lacks fixtures for alternative Cargo source types such as git dependencies or workspace-shared lockfiles, but the core lockfile gap is now covered.
- Risks or follow-up: the fixture is intentionally static and minimal, so it models realistic lockfile evidence without attempting to represent a full buildable application.

## [2026-06-08 19:03]

### Language

- `ruby`

### Workflow Assessment

- Current coverage: `bundler/` covered `Gemfile.lock` parsing and `bundle-vendor/` covered vendored `.gemspec` metadata, but there was no manifest-only Bundler fixture for direct `Gemfile` parsing.
- Gap reviewed: Bundler `Gemfile` manifest parsing, including grouped dependencies, multiple sources, and older option syntax still encountered in established Ruby repositories.
- Why it matters: scanners can currently succeed on Ruby in this corpus while missing dependencies declared only in `Gemfile` when no lockfile or vendored metadata is present.
- Decision: implemented `gemfile/` as a manifest-only Bundler fixture using a `Gemfile` with a primary source, a secondary source block, a development group, and an older hash-rocket option style.

### Research Sources

- RubyGems Guides, Gemfiles: https://bundler.io/guides/gemfile.html
- Bundler Gemfile manual: https://bundler.io/man/gemfile.5.html
- GitHub Advisory Database, `rack` advisory GHSA-gjh7-p2fx-99vx: https://github.com/advisories/GHSA-gjh7-p2fx-99vx

### Vulnerable Package Selection

- Package: `rack`
- Version: `3.1.13`
- Rationale: the GitHub advisory marks `rack` versions `>= 3.1, < 3.1.14` as affected by CVE-2025-46727, making `3.1.13` a precise historically vulnerable RubyGems version for a manifest fixture.

### Implementation

- Files added:
  - `fixtures/ruby/gemfile/Gemfile`
  - `fixtures/ruby/gemfile/expected.json`
- Files updated:
  - `fixtures/ruby/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: direct manifest extraction from a `Gemfile`, including one vulnerable runtime gem, one source-block gem, one development-group gem, and one dependency using older option syntax.

### expected.json Assessment

- Ecosystem label: `RubyGems`
- Discovery types used: `manifest`
- Validation notes: `expected.json` records only gems explicitly declared in `Gemfile` and preserves the direct declaration versions without inferring transitive dependencies.

### Documentation Impact

- Language README updates: added the `gemfile/` fixture row, expected package notes, and direct-manifest parsing behavior.
- Root documentation updates: updated the Ruby layout tree and coverage row to include `gemfile`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: direct `Gemfile` parsing, grouped Bundler dependency handling, additional source block handling, and tolerance for older hash-rocket option syntax in Ruby manifests.
- Remaining gaps: Ruby still lacks fixtures for alternative checked-in Bundler artifacts such as cached gem archives or custom install-path metadata, but the core manifest-only gap is now covered.
- Risks or follow-up: the fixture intentionally avoids a `Gemfile.lock` so scanners are tested on declaration parsing alone rather than lockfile-assisted resolution.

## [2026-06-08 18:58]

### Language

- `python`

### Workflow Assessment

- Current coverage: `pip/` covered plain requirements manifests, while `poetry/`, `pdm/`, `uv/`, and `conda_lock/` covered several lockfile formats. The corpus had no Pipenv coverage.
- Gap reviewed: `Pipfile` and `Pipfile.lock` support for Pipenv, including the `default` and `develop` lockfile sections.
- Why it matters: Pipenv is still widely encountered, and its lockfile is structurally different from `requirements.txt`, Poetry, PDM, and uv. Scanners that only understand those formats will miss a common Python dependency source.
- Decision: implemented `pipenv/` as a mixed manifest and lockfile fixture using `Pipfile` plus `Pipfile.lock`.

### Research Sources

- Pipenv documentation, `Pipfile` and `Pipfile.lock`: https://pipenv.pypa.io/en/latest/pipfile.html
- Pipenv documentation, `Pipfile.lock` structure and sections: https://pipenv.pypa.io/en/stable/pipfile.html
- GitHub Advisory Database, `urllib3` advisory GHSA-v845-jxx5-vc9f: https://github.com/advisories/GHSA-v845-jxx5-vc9f

### Vulnerable Package Selection

- Package: `urllib3`
- Version: `2.0.5`
- Rationale: the GitHub advisory marks `urllib3` versions `>= 2.0.0, < 2.0.6` as affected, making `2.0.5` a precise historically vulnerable version for a Pipenv fixture.

### Implementation

- Files added:
  - `fixtures/python/pipenv/Pipfile`
  - `fixtures/python/pipenv/Pipfile.lock`
  - `fixtures/python/pipenv/expected.json`
- Files updated:
  - `fixtures/python/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: mixed manifest and locked dependency extraction for Pipenv projects, including one direct vulnerable runtime package and development-only locked dependencies.

### expected.json Assessment

- Ecosystem label: `PyPI`
- Discovery types used: `manifest`, `locked`
- Validation notes: `expected.json` records direct declarations from `Pipfile` and exact locked versions from the `default` and `develop` sections of `Pipfile.lock`.

### Documentation Impact

- Language README updates: added the `pipenv/` fixture row, expected package notes, and `Pipfile.lock` section behavior.
- Root documentation updates: updated the Python layout tree and coverage row to include `pipenv`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: Pipenv manifest parsing, `Pipfile.lock` parsing, `default` versus `develop` dependency section handling, and differentiation between direct declarations and locked versions in Python fixtures.
- Remaining gaps: Python still lacks installed-package metadata fixtures such as checked-in `.dist-info` or site-packages metadata, but the major Pipenv package-manager gap is now covered.
- Risks or follow-up: the lockfile uses placeholder hashes because the corpus is testing static dependency discovery rather than lockfile integrity verification.

## [2026-06-08 18:52]

### Language

- `php`

### Workflow Assessment

- Current coverage: `composer/` covered lockfile parsing and `composer-vendor/` covered installed metadata only at the default `vendor/composer/installed.json` path.
- Gap reviewed: installed Composer package metadata when `config.vendor-dir` relocates the vendor tree away from the default `vendor/` directory.
- Why it matters: Composer explicitly supports a custom `vendor-dir`, so scanners that assume installed metadata only lives under `vendor/composer/installed.json` will miss real repositories.
- Decision: implemented `composer-custom-vendor/` as a minimal fixture using `composer.json` with `config.vendor-dir` set to `deps` and installed package metadata in `deps/composer/installed.json`.

### Research Sources

- Composer documentation, `config.vendor-dir`: https://getcomposer.org/doc/06-config.md#vendor-dir
- Composer upgrade guide, `vendor/composer/installed.json` format changes in Composer 2.0: https://getcomposer.org/upgrade/UPGRADE-2.0.md
- GitHub Advisory Database, `symfony/http-foundation` advisory GHSA-mrqx-rp3w-jpjp: https://github.com/advisories/GHSA-mrqx-rp3w-jpjp

### Vulnerable Package Selection

- Package: `symfony/http-foundation`
- Version: `6.4.13`
- Rationale: the GitHub advisory marks `symfony/http-foundation` versions `>= 6.0.0, < 6.4.14` as affected by CVE-2024-50345, making `6.4.13` a precise historically vulnerable Packagist version for an installed-metadata fixture.

### Implementation

- Files added:
  - `fixtures/php/composer-custom-vendor/composer.json`
  - `fixtures/php/composer-custom-vendor/deps/composer/installed.json`
  - `fixtures/php/composer-custom-vendor/expected.json`
- Files updated:
  - `fixtures/php/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: mixed manifest and installed detection for Composer projects that relocate the vendor tree with `config.vendor-dir`, including one installed-only package to test source attribution.

### expected.json Assessment

- Ecosystem label: `Packagist`
- Discovery types used: `manifest`, `installed`
- Validation notes: `expected.json` records the declared `symfony/http-foundation` package from `composer.json`, the installed `symfony/http-foundation` package from `deps/composer/installed.json`, and the installed-only `symfony/deprecation-contracts` package from the same metadata file.

### Documentation Impact

- Language README updates: added the `composer-custom-vendor/` fixture row, expected package notes, and the custom `vendor-dir` behavior.
- Root documentation updates: updated the PHP layout tree and coverage row to include `composer-custom-vendor/`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: Composer installed-package discovery outside the default `vendor/` path, Composer 2-style `installed.json` parsing with top-level `packages`, and distinction between declared and installed package evidence in relocated vendor trees.
- Remaining gaps: PHP still lacks alternative installed metadata forms such as runtime-oriented Composer metadata beyond `installed.json`, but the custom vendor directory gap is now covered.
- Risks or follow-up: the installed metadata is intentionally minimal and static, so it tests dependency discovery against realistic file placement rather than a fully materialized Composer install.

## [2026-06-08 13:15]

### Language

- `csharp`

### Workflow Assessment

- Current coverage: `legacy/`, `msbuild/`, and `lockfile/` covered manifest and lockfile parsing, but C# still had no installed-package metadata fixture.
- Gap reviewed: checked-in NuGet `packages/` folder metadata driven by `packages.config` and `repositoryPath`.
- Why it matters: `packages.config`-style projects can keep a local `packages/` folder, and extracted `.nuspec` files provide installed package identity that is distinct from direct declarations and lock-state parsing.
- Decision: implemented a `packages-folder/` fixture combining `packages.config`, `nuget.config`, and extracted `.nuspec` files under `packages/`.

### Research Sources

- Microsoft Learn, `nuget.config` reference: https://learn.microsoft.com/en-us/nuget/reference/nuget-config-file
- Microsoft Learn, `.nuspec` file reference: https://learn.microsoft.com/en-us/nuget/reference/nuspec
- GitHub Advisory Database, `Newtonsoft.Json` advisory GHSA-5crp-9r3c-p9vr: https://github.com/advisories/GHSA-5crp-9r3c-p9vr

### Vulnerable Package Selection

- Package: `Newtonsoft.Json`
- Version: `12.0.3`
- Rationale: `Newtonsoft.Json` versions earlier than `13.0.1` are affected by CVE-2024-21907, and `12.0.3` is a recognizable historically vulnerable NuGet version that fits a packages-folder fixture well.

### Implementation

- Files added:
  - `fixtures/csharp/packages-folder/nuget.config`
  - `fixtures/csharp/packages-folder/packages.config`
  - `fixtures/csharp/packages-folder/expected.json`
  - `fixtures/csharp/packages-folder/packages/Newtonsoft.Json.12.0.3/Newtonsoft.Json.nuspec`
  - `fixtures/csharp/packages-folder/packages/Microsoft.Extensions.Logging.Abstractions.5.0.0/Microsoft.Extensions.Logging.Abstractions.nuspec`
- Files updated:
  - `fixtures/csharp/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: mixed manifest and installed NuGet package detection using `packages.config` declarations plus extracted installed-package metadata from a custom `repositoryPath`.

### expected.json Assessment

- Ecosystem label: `NuGet`
- Discovery types used: `manifest`, `installed`
- Validation notes: `expected.json` records the declared `Newtonsoft.Json` package from `packages.config`, the installed `Newtonsoft.Json` package from `.nuspec` metadata, and one installed-only package to test asymmetry handling.

### Documentation Impact

- Language README updates: added the `packages-folder/` fixture row, expected package notes, and installed-metadata behavior.
- Root documentation updates: updated the C# layout tree and coverage row to include `packages-folder`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: local `packages/` folder discovery, `.nuspec`-based installed package extraction, and differentiation between direct `packages.config` declarations and checked-in installed NuGet metadata.
- Remaining gaps: C# still lacks broader restore-artifact scenarios such as `project.assets.json`, though the installed-metadata gap is now covered.
- Risks or follow-up: the installed package metadata is intentionally minimal and does not attempt to model the full extracted package layout because the corpus is testing dependency-discovery inputs, not package execution.

## [2026-06-08 19:24]

### Language

- `node`

### Workflow Assessment

- Current coverage: `bun/`, `deno/`, `npm/`, and `pnpm/` already covered four Node lockfile styles, but the repository still had no Yarn fixture and no Node sample mixing `manifest` and `locked` evidence.
- Gap reviewed: Yarn Classic dependency coverage via `package.json` plus `yarn.lock`.
- Why it matters: Yarn remains a first-class package manager, and its v1 lockfile format uses a bespoke selector syntax that differs materially from the JSON and YAML lockfiles already present in the corpus.
- Decision: implemented `yarn/` as a minimal Yarn Classic fixture with grouped selectors and one direct dependency that is also present transitively.

### Research Sources

- Yarn home page: https://yarnpkg.com/
- Yarn Classic docs, `yarn.lock`: https://classic.yarnpkg.com/lang/en/docs/yarn-lock/
- GitHub Advisory Database, `minimist` prototype pollution / GHSA-vh95-rmgr-6w4m: https://github.com/advisories/GHSA-vh95-rmgr-6w4m

### Vulnerable Package Selection

- Package: `minimist`
- Version: `0.0.8`
- Rationale: `minimist` is a common npm package, the GitHub advisory lists versions earlier than `0.2.1` as affected, and `0.0.8` is a simple historically vulnerable version that also appears in classic transitive trees such as `mkdirp` `0.5.1`.

### Implementation

- Files added:
  - `fixtures/node/yarn/package.json`
  - `fixtures/node/yarn/yarn.lock`
  - `fixtures/node/yarn/expected.json`
- Files updated:
  - `fixtures/node/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: manifest and locked dependency extraction from a Yarn Classic project, including grouped selector parsing in `yarn.lock` and comparison between a declared semver range and an exact locked package version.

### expected.json Assessment

- Ecosystem label: `node`
- Discovery types used: `manifest`, `locked`
- Validation notes: `expected.json` includes only the direct dependencies declared in `package.json` and the exact packages explicitly represented in `yarn.lock`, with duplicate package names preserved where the manifest range and locked version intentionally differ.

### Documentation Impact

- Language README updates: added the `yarn/` fixture row, expected dependency notes, and the grouped-selector rationale.
- Root documentation updates: updated the Node layout tree and coverage row to include `yarn/`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: Yarn Classic v1 lockfile parsing, grouped selector normalization, and mixed `manifest` plus `locked` Node dependency extraction.
- Remaining gaps: Node still lacks coverage for npm `npm-shrinkwrap.json` precedence and modern Yarn zero-install cache metadata.
- Risks or follow-up: the `resolved` and `integrity` values are placeholders because the fixture is intended for static parsing tests rather than installation fidelity.

## [2026-06-08 19:29]

### Language

- `node`

### Workflow Assessment

- Current coverage: `bun/`, `deno/`, `npm/`, `pnpm/`, and `yarn/` already covered the main lockfile families, but the repository still had no fixture for npm's publishable `npm-shrinkwrap.json` or its precedence over `package-lock.json`.
- Gap reviewed: npm shrinkwrap coverage via `package.json`, `npm-shrinkwrap.json`, and a conflicting `package-lock.json`.
- Why it matters: npm documents `npm-shrinkwrap.json` as a distinct publishable lockfile that takes precedence over `package-lock.json`, which is a behavior scanners can easily miss if they treat both files as equivalent or additive.
- Decision: implemented `npm-shrinkwrap/` as a minimal precedence fixture with one direct dependency and one transitive vulnerable package.

### Research Sources

- npm Docs, `npm-shrinkwrap.json`: https://docs.npmjs.com/cli/v7/configuring-npm/npm-shrinkwrap-json/
- npm Docs, `npm install`: https://docs.npmjs.com/cli/v11/commands/npm-install/
- npm Docs, `package-lock.json`: https://docs.npmjs.com/cli/v11/configuring-npm/package-lock-json/
- GitHub Advisory Database, `minimist` prototype pollution / GHSA-vh95-rmgr-6w4m: https://github.com/advisories/GHSA-vh95-rmgr-6w4m

### Vulnerable Package Selection

- Package: `minimist`
- Version: `0.0.8`
- Rationale: the GitHub advisory marks versions earlier than `0.2.1` as affected, and `0.0.8` is a recognizable historical version that fits naturally as a transitive dependency under `mkdirp` `0.5.1`.

### Implementation

- Files added:
  - `fixtures/node/npm-shrinkwrap/package.json`
  - `fixtures/node/npm-shrinkwrap/npm-shrinkwrap.json`
  - `fixtures/node/npm-shrinkwrap/package-lock.json`
  - `fixtures/node/npm-shrinkwrap/expected.json`
- Files updated:
  - `fixtures/node/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: mixed manifest and locked detection for npm shrinkwrap projects, plus precedence validation where `npm-shrinkwrap.json` intentionally conflicts with `package-lock.json`.

### expected.json Assessment

- Ecosystem label: `node`
- Discovery types used: `manifest`, `locked`
- Validation notes: `expected.json` records the direct `mkdirp` declaration from `package.json` and the exact versions from `npm-shrinkwrap.json` only; the conflicting `package-lock.json` is intentionally excluded because npm documents it as ignored when a shrinkwrap file is present.

### Documentation Impact

- Language README updates: added the `npm-shrinkwrap/` fixture row, expected dependency notes, and the precedence behavior under test.
- Root documentation updates: updated the Node layout tree and coverage row to include `npm-shrinkwrap/`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: publishable npm shrinkwrap parsing, `npm-shrinkwrap.json` versus `package-lock.json` precedence, and mixed manifest-plus-lockfile extraction for Node fixtures.
- Remaining gaps: Node still lacks a fixture for modern Yarn zero-install metadata or other installed-package evidence committed into the repository.
- Risks or follow-up: the conflicting `package-lock.json` is intentionally synthetic and should remain documented as an ignored precedence test rather than a second valid source of truth.

## [2026-06-08 19:35]

### Language

- `node`

### Workflow Assessment

- Current coverage: `bun/`, `deno/`, `npm/`, `npm-shrinkwrap/`, `pnpm/`, and `yarn/` covered lockfile and manifest cases, but Node still lacked any fixture for checked-in installed package metadata.
- Gap reviewed: vendored `node_modules` coverage via top-level `package.json` plus nested `node_modules/**/package.json` files.
- Why it matters: npm documents local installs into `./node_modules`, and packages are identified by their `package.json` files, so scanners that only inspect manifests and lockfiles will miss repositories that commit installed dependency metadata.
- Decision: implemented `node-modules/` as a minimal installed-metadata fixture with one declared direct dependency and one nested transitive installed package.

### Research Sources

- npm Docs, "About packages and modules": https://docs.npmjs.com/about-packages-and-modules/
- npm Docs, "Folders": https://docs.npmjs.com/cli/v11/configuring-npm/folders/
- GitHub Advisory Database, `minimist` prototype pollution / GHSA-vh95-rmgr-6w4m: https://github.com/advisories/GHSA-vh95-rmgr-6w4m

### Vulnerable Package Selection

- Package: `minimist`
- Version: `0.0.8`
- Rationale: GitHub marks `minimist` versions earlier than `0.2.1` as affected, and `0.0.8` is a historically recognizable version that also fits a realistic nested dependency under `mkdirp` `0.5.1`.

### Implementation

- Files added:
  - `fixtures/node/node-modules/package.json`
  - `fixtures/node/node-modules/node_modules/mkdirp/package.json`
  - `fixtures/node/node-modules/node_modules/mkdirp/node_modules/minimist/package.json`
  - `fixtures/node/node-modules/expected.json`
- Files updated:
  - `fixtures/node/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: mixed manifest and installed detection for Node projects that check `node_modules` metadata into source control, including nested transitive installed-package discovery.

### expected.json Assessment

- Ecosystem label: `node`
- Discovery types used: `manifest`, `installed`
- Validation notes: `expected.json` records the direct declaration from the top-level `package.json` and only the exact package identities present in the checked-in `node_modules/**/package.json` files.

### Documentation Impact

- Language README updates: added the `node-modules/` fixture row, expected dependency notes, and the nested installed-metadata behavior under test.
- Root documentation updates: updated the Node layout tree and coverage row to include `node-modules/`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: checked-in `node_modules` package metadata parsing, nested installed dependency traversal, and distinction between direct manifest intent and installed Node package versions.
- Remaining gaps: Node still lacks a fixture for modern Yarn zero-install metadata or workspace-oriented dependency declarations.
- Risks or follow-up: the fixture intentionally models only package identity metadata, not a complete runnable `node_modules` tree, so future additions should keep that same minimal scope.

## [2026-06-08 19:38]

### Language

- `python`

### Workflow Assessment

- Current coverage: `pip/`, `pipenv/`, `poetry/`, `pdm/`, `uv/`, and `conda_lock/` covered manifest and lockfile inputs, but Python still lacked any installed-package metadata fixture.
- Gap reviewed: checked-in `.dist-info` metadata under `site-packages/`, paired with a top-level `requirements.txt`.
- Why it matters: PyPA specifies `.dist-info` directories and a mandatory `METADATA` file for installed projects, so scanners that only inspect `requirements.txt` and lockfiles will miss repositories that include Python installation metadata.
- Decision: implemented `dist-info/` as a minimal mixed manifest and installed fixture using `requirements.txt` plus two `.dist-info/METADATA` files.

### Research Sources

- Python Packaging User Guide, "Recording installed projects": https://packaging.python.org/en/latest/specifications/recording-installed-packages/
- pip documentation, "Requirements File Format": https://pip.pypa.io/en/stable/reference/requirements-file-format/
- GitHub Advisory Database, `urllib3` advisory GHSA-34jh-p97f-mpxf: https://github.com/advisories/GHSA-34jh-p97f-mpxf

### Vulnerable Package Selection

- Package: `urllib3`
- Version: `1.26.16`
- Rationale: GitHub marks `urllib3` versions earlier than `1.26.19` as affected by GHSA-34jh-p97f-mpxf, and `1.26.16` is a recognizable historically vulnerable version that fits naturally as installed metadata alongside `requests` `2.31.0`.

### Implementation

- Files added:
  - `fixtures/python/dist-info/requirements.txt`
  - `fixtures/python/dist-info/site-packages/requests-2.31.0.dist-info/METADATA`
  - `fixtures/python/dist-info/site-packages/urllib3-1.26.16.dist-info/METADATA`
  - `fixtures/python/dist-info/expected.json`
- Files updated:
  - `fixtures/python/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: mixed manifest and installed detection for Python projects that check `.dist-info` metadata into source control, including an installed-only transitive package version.

### expected.json Assessment

- Ecosystem label: `PyPI`
- Discovery types used: `manifest`, `installed`
- Validation notes: `expected.json` records the direct `requests` declaration from `requirements.txt` and only the exact package identities present in the checked-in `.dist-info/METADATA` files.

### Documentation Impact

- Language README updates: added the `dist-info/` fixture row, expected dependency notes, and the installed-metadata behavior under test.
- Root documentation updates: updated the Python layout tree and coverage row to include `dist-info/`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: `.dist-info/METADATA` parsing, checked-in `site-packages` metadata discovery, and distinction between direct requirements declarations and installed Python package versions.
- Remaining gaps: Python still lacks legacy `.egg-info` installed metadata coverage and a `pyproject.toml` manifest-only fixture.
- Risks or follow-up: the fixture intentionally models only mandatory package identity metadata rather than a complete environment, so future installed-package fixtures should keep the same minimal scope.

## [2026-06-08 19:44]

### Language

- `java`

### Workflow Assessment

- Current coverage: `gradle/`, `gradle-lockfile/`, and `maven/` covered manifest and lock-state inputs, but Java still lacked any vendored or installed repository metadata fixture.
- Gap reviewed: checked-in Maven repository metadata via standard repository-layout `*.pom` files alongside a top-level `pom.xml`.
- Why it matters: Maven repositories store artifacts in a defined coordinate-based layout, and vendored repository POM files provide installed package identity separate from direct manifest declarations.
- Decision: implemented `maven-repo/` as a minimal mixed manifest and installed fixture using `pom.xml` plus two vendored repository POMs.

### Research Sources

- Apache Maven, "Maven Repository Layout": https://maven.apache.org/repositories/layout.html
- Apache Maven, "Introduction to Repositories": https://maven.apache.org/guides/introduction/introduction-to-repositories
- GitHub Advisory Database, Log4Shell / GHSA-jfh8-c2jp-5v3q: https://github.com/advisories/GHSA-jfh8-c2jp-5v3q

### Vulnerable Package Selection

- Package: `org.apache.logging.log4j:log4j-core`
- Version: `2.14.1`
- Rationale: `2.14.1` is the widely recognized Log4Shell-era vulnerable version, and it fits naturally in a vendored Maven repository fixture with `log4j-api` `2.14.1` as installed-only companion metadata.

### Implementation

- Files added:
  - `fixtures/java/maven-repo/pom.xml`
  - `fixtures/java/maven-repo/repository/org/apache/logging/log4j/log4j-core/2.14.1/log4j-core-2.14.1.pom`
  - `fixtures/java/maven-repo/repository/org/apache/logging/log4j/log4j-api/2.14.1/log4j-api-2.14.1.pom`
  - `fixtures/java/maven-repo/expected.json`
- Files updated:
  - `fixtures/java/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: mixed manifest and installed detection for vendored Maven repository metadata, including one declared-and-installed package and one installed-only package.

### expected.json Assessment

- Ecosystem label: `Maven`
- Discovery types used: `manifest`, `installed`
- Validation notes: `expected.json` records the direct coordinate from the top-level `pom.xml` and only the exact package identities present in the vendored repository `*.pom` files.

### Documentation Impact

- Language README updates: added the `maven-repo/` fixture row, expected dependency notes, and the vendored repository behavior under test.
- Root documentation updates: updated the Java layout tree and coverage row to include `maven-repo/`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: vendored Maven repository layout parsing, installed package discovery from checked-in `*.pom` metadata, and distinction between direct Maven declarations and installed Java package versions.
- Remaining gaps: Java still lacks Gradle cache metadata or alternative repository-manager export formats, but the core installed-metadata gap is now covered.
- Risks or follow-up: the fixture intentionally models repository POM identity only, not full artifact or JAR contents, so future Java installed-package fixtures should keep the same minimal scope.

## [2026-06-08 19:49]

### Language

- `go`

### Workflow Assessment

- Current coverage: `mod/` covered a single-module `go.mod` plus `go.sum`, and `vendor/` covered vendored module metadata from `vendor/modules.txt`, but Go still lacked any workspace-oriented fixture.
- Gap reviewed: multi-module workspace coverage via `go.work` with more than one module root.
- Why it matters: the Go toolchain treats the modules listed in `go.work` as the main modules of a single workspace, so scanners that stop at the first `go.mod` file will miss dependencies that are part of the active workspace.
- Decision: implemented `workspace/` as a minimal multi-module fixture using `go.work` plus two workspace module directories.

### Research Sources

- Go Modules Reference, workspaces and `go.work`: https://go.dev/ref/mod
- Go tutorial, multi-module workspaces: https://go.dev/doc/tutorial/workspaces
- Go vulnerability report GO-2023-1571: https://pkg.go.dev/vuln/GO-2023-1571

### Vulnerable Package Selection

- Package: `golang.org/x/net`
- Version: `0.6.0`
- Rationale: the Go vulnerability report marks `golang.org/x/net/http2` and `golang.org/x/net/http2/hpack` as affected before `v0.7.0`, so `v0.6.0` remains a clean historically vulnerable version for a workspace fixture.

### Implementation

- Files added:
  - `fixtures/go/workspace/go.work`
  - `fixtures/go/workspace/api/go.mod`
  - `fixtures/go/workspace/api/go.sum`
  - `fixtures/go/workspace/tools/go.mod`
  - `fixtures/go/workspace/tools/go.sum`
  - `fixtures/go/workspace/expected.json`
- Files updated:
  - `fixtures/go/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: locked module discovery across multiple main modules defined by a checked-in `go.work` file, including one vulnerable module and one dependency that exists only in the second workspace module.

### expected.json Assessment

- Ecosystem label: `Go`
- Discovery types used: `locked`
- Validation notes: `expected.json` records only module versions explicitly present in the workspace modules’ `go.sum` files, with one unioned result set for the whole workspace.

### Documentation Impact

- Language README updates: added the `workspace/` fixture row, expected module notes, and the workspace aggregation behavior under test.
- Root documentation updates: updated the Go layout tree and coverage row to include `workspace/`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: `go.work` parsing, aggregation of multiple workspace modules, and union of locked module versions across more than one `go.mod`/`go.sum` pair.
- Remaining gaps: Go still lacks legacy GOPATH-style dependency metadata or cache-oriented module metadata, but the high-value workspace gap is now covered.
- Risks or follow-up: the fixture intentionally omits source packages and `go.work.sum` to keep the scenario narrow and focused on workspace-level dependency discovery.

## [2026-06-08 20:13]

### Language

- `node`

### Workflow Assessment

- Current coverage: Node already covered npm, shrinkwrap, pnpm, Bun, Deno, checked-in `node_modules`, and Yarn Classic `yarn.lock` v1.
- Gap reviewed: modern Yarn Berry lockfiles and Plug'n'Play-oriented repository metadata.
- Why it matters: Yarn Berry uses a different lockfile shape from Yarn Classic, adds a top-level `__metadata` block, and commonly pairs lockfiles with `.yarnrc.yml` PnP configuration.
- Decision: implemented `yarn-berry/` as a manifest-plus-lockfile fixture with Berry-style metadata and a minimal PnP configuration marker.

### Research Sources

- Yarn, "Plug'n'Play": https://yarnpkg.com/features/pnp
- Yarn, "`yarn install`": https://yarnpkg.com/cli/install
- Yarn, "Manifest (`package.json`)": https://yarnpkg.com/configuration/manifest
- OSV, `GHSA-xxjr-mmjv-4gpg`: https://test.osv.dev/vulnerability/GHSA-xxjr-mmjv-4gpg

### Vulnerable Package Selection

- Package: `lodash`
- Version: `4.17.20`
- Rationale: `lodash` is widely recognized in npm dependency graphs, and the advisory covers versions through `4.17.22`, making `4.17.20` a clean vulnerable direct dependency for a modern Yarn fixture.

### Implementation

- Files added:
  - `fixtures/node/yarn-berry/package.json`
  - `fixtures/node/yarn-berry/.yarnrc.yml`
  - `fixtures/node/yarn-berry/yarn.lock`
  - `fixtures/node/yarn-berry/expected.json`
- Files updated:
  - `fixtures/node/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: modern Yarn lockfile parsing using Berry-style `__metadata`, `resolution`, `checksum`, and `linkType` fields alongside explicit PnP configuration in `.yarnrc.yml`.

### expected.json Assessment

- Ecosystem label: `node`
- Discovery types used: `manifest`, `locked`
- Validation notes: the expected entries are limited to direct packages declared in `package.json` and exact package versions represented in the checked-in `yarn.lock`.

### Documentation Impact

- Language README updates: added the `yarn-berry/` fixture row, expected package notes, and a short explanation of the modern lockfile shape.
- Root documentation updates: updated the Node layout tree and coverage row to include `yarn-berry/`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: Yarn Berry lockfile parsing, direct-versus-locked comparison for modern Yarn, and detection of Plug'n'Play configuration through `.yarnrc.yml`.
- Remaining gaps: Node still does not model a full checked-in `.pnp.cjs` or zero-install cache layout.
- Risks or follow-up: the lockfile checksum values are placeholders because this fixture is designed for static parsing rather than install replay.

## [2026-06-08 20:15]

### Language

- `csharp`

### Workflow Assessment

- Current coverage: C# already covered `packages.config`, SDK-style `PackageReference`, a NuGet lockfile, and installed package metadata from a `packages/` folder.
- Gap reviewed: Paket's repository-level dependency files and lockfile syntax.
- Why it matters: Paket is a distinct .NET package manager workflow with its own declaration, lock, and per-project reference files, all of which differ materially from native NuGet formats.
- Decision: implemented `paket/` as a minimal NuGet-backed Paket fixture using `paket.dependencies`, `paket.lock`, and `paket.references`.

### Research Sources

- Paket, "What is Paket?": https://fsprojects.github.io/Paket/
- Paket, "The paket.lock file": https://fsprojects.github.io/Paket/lock-file.html
- Paket, "The paket.references files": https://fsprojects.github.io/Paket/references-files.html
- OSV, `GHSA-8rfx-6mr3-5jh3`: https://osv.dev/vulnerability/GHSA-8rfx-6mr3-5jh3

### Vulnerable Package Selection

- Package: `Newtonsoft.Json`
- Version: `12.0.3`
- Rationale: the advisory covers versions before `13.0.1`, and `12.0.3` is already a recognizable vulnerable NuGet version elsewhere in the corpus, which keeps the new fixture focused on Paket file semantics rather than package selection churn.

### Implementation

- Files added:
  - `fixtures/csharp/paket/paket.dependencies`
  - `fixtures/csharp/paket/paket.lock`
  - `fixtures/csharp/paket/src/App/App.csproj`
  - `fixtures/csharp/paket/src/App/paket.references`
  - `fixtures/csharp/paket/expected.json`
- Files updated:
  - `fixtures/csharp/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: direct dependency extraction from `paket.dependencies` plus locked direct and transitive NuGet package extraction from `paket.lock`, with `paket.references` constraining the project-level direct package set.

### expected.json Assessment

- Ecosystem label: `NuGet`
- Discovery types used: `manifest`, `locked`
- Validation notes: the manifest entries come from exact version declarations in `paket.dependencies`, and the locked entries are limited to packages explicitly present in `paket.lock`.

### Documentation Impact

- Language README updates: added the `paket/` fixture row, expected packages, and notes describing how Paket's files relate to direct and transitive package discovery.
- Root documentation updates: updated the C# layout tree and coverage row to include `paket/`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: Paket dependency-file parsing, Paket lockfile traversal, and differentiation between repository-level declarations and per-project direct references in C# fixtures.
- Remaining gaps: C# still lacks Central Package Management coverage via `Directory.Packages.props`.
- Risks or follow-up: the fixture intentionally reuses the existing logging-related NuGet package graph so the new test surface stays on Paket format handling.

## [2026-06-08 20:16]

### Language

- `java`

### Workflow Assessment

- Current coverage: Java already covered Gradle manifests, Gradle lockfiles, Maven manifests, and vendored Maven repository metadata.
- Gap reviewed: Apache Ivy descriptor coverage.
- Why it matters: Ivy is a distinct Java dependency manager with its own XML schema and attribute naming, so scanners that only understand Maven or Gradle conventions can miss it entirely.
- Decision: implemented `ivy/` as a minimal manifest-only fixture centered on `ivy.xml`.

### Research Sources

- Apache Ivy, "FAQ": https://ant.apache.org/ivy/faq.html
- Apache Ivy, "Ivy Files": https://ant.apache.org/ivy/history/latest-milestone/ivyfile.html
- Apache Ivy, "Quick Start": https://ant.apache.org/ivy/history/master/tutorial/start.html
- OSV, `GHSA-p6xc-xr62-6r2g`: https://osv.dev/vulnerability/GHSA-p6xc-xr62-6r2g

### Vulnerable Package Selection

- Package: `org.apache.logging.log4j:log4j-core`
- Version: `2.14.1`
- Rationale: `2.14.1` is the widely recognized Log4Shell-era vulnerable version, and it keeps the Ivy fixture aligned with the corpus' existing Java package choices.

### Implementation

- Files added:
  - `fixtures/java/ivy/ivy.xml`
  - `fixtures/java/ivy/expected.json`
- Files updated:
  - `fixtures/java/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: manifest dependency extraction from Apache Ivy `org`/`name`/`rev` attributes, including configuration-qualified dependency declarations.

### expected.json Assessment

- Ecosystem label: `Maven`
- Discovery types used: `manifest`
- Validation notes: expected package names are normalized as `groupId:artifactId`, consistent with the existing Java fixtures.

### Documentation Impact

- Language README updates: added the `ivy/` fixture row, expected packages, and notes describing how Ivy differs from Maven and Gradle.
- Root documentation updates: updated the Java layout tree and coverage row to include `ivy/`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: Ivy descriptor parsing, dependency extraction from `ivy.xml`, and normalization of Ivy coordinates into the repository's existing `Maven` ecosystem label.
- Remaining gaps: Java still lacks Gradle version catalog coverage.
- Risks or follow-up: this fixture intentionally stays manifest-only because the value is the Ivy descriptor shape rather than a larger Ant or published-repository workflow.

## [2026-06-08 20:18]

### Language

- `python`

### Workflow Assessment

- Current coverage: Python already covered `requirements.txt`, `Pipfile`, `Pipfile.lock`, Poetry, PDM, uv, Conda lockfiles, and checked-in `.dist-info` metadata.
- Gap reviewed: generic `pyproject.toml` dependency declarations outside tool-specific lockfile formats.
- Why it matters: `pyproject.toml` has become the standard Python project metadata surface, and scanners that only recognize Poetry or PDM files can miss plain PEP 621 dependency declarations.
- Decision: implemented `pyproject/` as a minimal manifest-only fixture using the standardized `[project].dependencies` array.

### Research Sources

- PyPA, "`pyproject.toml` specification": https://packaging.python.org/en/latest/specifications/pyproject-toml/
- pip, "Requirements File Format": https://pip.pypa.io/en/stable/reference/requirements-file-format/
- OSV, `PYSEC-2026-141`: https://osv.dev/vulnerability/PYSEC-2026-141
- OSV, `GHSA-hj2j-77xm-mc5v`: https://osv.dev/vulnerability/GHSA-hj2j-77xm-mc5v

### Vulnerable Package Selection

- Package: `urllib3`
- Version: `1.26.16`
- Rationale: the PyPA advisory database marks `urllib3` `1.26.16` as affected in a broad vulnerable range, and it is already a recognizable Python dependency elsewhere in the corpus.

### Implementation

- Files added:
  - `fixtures/python/pyproject/pyproject.toml`
  - `fixtures/python/pyproject/expected.json`
- Files updated:
  - `fixtures/python/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: direct dependency extraction from PEP 621 TOML requirement strings without any accompanying lockfile or installer-specific metadata.

### expected.json Assessment

- Ecosystem label: `PyPI`
- Discovery types used: `manifest`
- Validation notes: the expected versions are exact pins taken directly from the TOML dependency strings, which keeps the manifest-only fixture deterministic.

### Documentation Impact

- Language README updates: added the `pyproject/` fixture row, expected package notes, and an explanation of why a plain `pyproject.toml` fixture matters separately from Poetry or PDM.
- Root documentation updates: updated the Python layout tree and coverage row to include `pyproject/`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: generic PEP 621 dependency parsing, extraction of direct dependencies from TOML string entries, and separation of manifest-only `pyproject.toml` support from Poetry/PDM lockfile support.
- Remaining gaps: Python package-manager breadth now looks solid, with remaining work mostly limited to additional edge-case formats rather than major managers.
- Risks or follow-up: this fixture intentionally avoids optional dependency groups and tool tables to stay focused on the core standardized dependency surface.

## [2026-06-08 20:19]

### Language

- `csharp`

### Workflow Assessment

- Current coverage: C# already covered native NuGet declarations, lockfiles, installed package metadata, and Paket after the earlier fixture addition in this session.
- Gap reviewed: NuGet Central Package Management via `Directory.Packages.props`.
- Why it matters: CPM moves package versions out of project files, so scanners that only inspect `PackageReference Version=` attributes will miss direct dependencies or report incomplete versions.
- Decision: implemented `central-packages/` as a manifest-only fixture with shared versions in `Directory.Packages.props` and versionless `PackageReference` items in the project file.

### Research Sources

- Microsoft Learn, "Central Package Management": https://learn.microsoft.com/en-us/nuget/consume-packages/central-package-management
- Microsoft Learn, "Directory.Packages.props files imported by default": https://learn.microsoft.com/en-us/dotnet/core/compatibility/sdk/5.0/directory-packages-props-imported-by-default
- OSV, `GHSA-8rfx-6mr3-5jh3`: https://osv.dev/vulnerability/GHSA-8rfx-6mr3-5jh3

### Vulnerable Package Selection

- Package: `Newtonsoft.Json`
- Version: `12.0.3`
- Rationale: the advisory covers versions before `13.0.1`, and reusing `12.0.3` keeps the CPM fixture focused on central version-resolution behavior rather than introducing a new NuGet package graph.

### Implementation

- Files added:
  - `fixtures/csharp/central-packages/Directory.Packages.props`
  - `fixtures/csharp/central-packages/src/App/App.csproj`
  - `fixtures/csharp/central-packages/expected.json`
- Files updated:
  - `fixtures/csharp/README.md`
  - `README.md`
  - `.gitattributes`
- Fixture behavior introduced: direct package extraction where package IDs come from versionless `PackageReference` items and versions come from repository-level `PackageVersion` entries.

### expected.json Assessment

- Ecosystem label: `NuGet`
- Discovery types used: `manifest`
- Validation notes: the expected entries record versions from `Directory.Packages.props`, not from the project file, because that is the behavior under test.

### Documentation Impact

- Language README updates: added the `central-packages/` fixture row, expected packages, and notes explaining the version-joining behavior scanners must handle.
- Root documentation updates: updated the C# layout tree and coverage row to include `central-packages/`.
- Template or schema updates: none required.

### Repository Impact

- Scanner behaviors now covered: NuGet Central Package Management parsing, joining `PackageReference` identities with shared `PackageVersion` declarations, and manifest-only version attribution outside the project file.
- Remaining gaps: C# package-manager breadth now covers both native NuGet and Paket, with remaining work mostly limited to niche legacy layouts.
- Risks or follow-up: the fixture intentionally uses a single project so the central-versioning behavior remains obvious and reviewable.
